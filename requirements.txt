# The AutoML engine (models + search) is pure standard library — nothing needed.
# These are only for the serving API:
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
# Optional comparison against real AutoML:
# scikit-learn>=1.3
# tpot>=0.12
